{
  "name": "wwf-frontend",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "rm -rf ../backend/static/* && cp -r dist/* ../backend/static/",
    "cypress": "cypress run --headless"
  }
}

